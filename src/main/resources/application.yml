# 应用名称
spring:
  application:
    name: demo01
  datasource:
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db2?serverTimezone=UTC&characterEncoding=utf-8

server:
  port: 8081

swagger:
 enabled: true


#mybatis-plus:
#
# type-aliases-package: com.denglu.demo.entity
# mapper-locations: classpath:mappers/*Mapper.xml

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.denglu.demo.entity